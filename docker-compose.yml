services:
  bot:
    build: .
    image: bot-preset-fetcher:latest
    container_name: bot-preset-fetcher
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
